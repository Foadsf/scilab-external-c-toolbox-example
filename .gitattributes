# Set default behavior, in case users don't have core.autocrlf set.
* text=auto

# Explicitly declare text files you want to always normalize and handle line endings correctly.
*.c text
*.h text
*.cpp text
*.hpp text
*.hxx text
*.xml text
*.xsl text
*.sci text
*.sce text
*.po text
*.txt text
*.md text
*.gitignore text
*.gitattributes text

# Explicitly declare files that must have LF line endings on checkout.
*.sh text eol=lf

# Declare files that are binary and should not be modified.
# Add any other binary formats you might have.
*.png binary
*.jar binary
# *.mo binary # Often treated as binary, uncomment if needed
# *.bin binary # Treat scilab .bin as binary